package co.com.sofka.mycalendar.actividades;

import co.com.sofka.mycalendar.interfaces.Programable;
import co.com.sofka.mycalendar.jornada.*;

import java.time.LocalDate;
import java.time.LocalTime;

public class Lectura //extends Actividad implements Programable {
{
    /*
    public Lectura(String leerMiLibroFavorito, LocalDate localTime, LocalDate time) {
        super(titulo, fechaInicial, fechaFinal);
        if(titulo.isBlank()){
            throw new IllegalArgumentException("El titulo no es valido");
        }
    }

    public Lectura(String titulo, LocalDate fechaFinal)
    {

        this("Leer mi libro favorito",
                new Manana(10, 30).horaInicial(), new Manana(11, 00).horaFinal());


    }

    @Override
    public void agregarRepeticion(int repeticiones) {

    }

    */
}
